import 'package:flutter/material.dart';

class SliceProps {
  String description;
  Color backgroundColor;
  String backgroundImage;
  String image;

  SliceProps(
      this.backgroundColor, this.backgroundImage, this.description, this.image);
}
